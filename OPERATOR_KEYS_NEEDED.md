# Operator keys needed

> _Generated at: 2026-07-23T00:41:45.198Z_
> _Auto-generated by `scripts/gen-operator-keys.ts`. Do not edit by hand;
> re-run `npm run readiness:keys` to refresh._

This file lists every env var / provider credential the operator must
provide before the next production deploy. Three sections, three actions:

## 1. MUST_SET registry switches that are unset
Set each of these in Fly secrets before deploy:

| Switch | Owner | Tier | Description | Fly command |
|---|---|---|---|---|
| `DATABASE_URL` | platform | hard | Supabase pgbouncer pooler URL (port 6543, ?pgbouncer=true). Used by the runtime app for all query tr | `fly secrets set DATABASE_URL=<value>` |
| `DIRECT_URL` | platform | hard | Supabase direct connection URL (port 5432, no pgbouncer). Used only by `prisma migrate deploy` in th | `fly secrets set DIRECT_URL=<value>` |
| `RECENT_AUTH_SECRET` | auth | prod | Phase 10 — HMAC signing secret for short-lived re-auth tokens (X-Recent-Auth-Token). Required for ac | `fly secrets set RECENT_AUTH_SECRET=<value>` |
| `RECENT_AUTH_TTL_MS` | auth | prod | Phase 10 — validity window for re-auth tokens, in milliseconds. Must be a finite integer in [60000,  | `fly secrets set RECENT_AUTH_TTL_MS=<value>` |
| `SUPABASE_SERVICE_ROLE_KEY` | platform | hard | Supabase service-role key for server-side admin calls. | `fly secrets set SUPABASE_SERVICE_ROLE_KEY=<value>` |
| `SUPABASE_URL` | platform | hard | Supabase project URL for JWKS verification + admin API. | `fly secrets set SUPABASE_URL=<value>` |
| `FLY_API_TOKEN` | platform | prod | Fly.io API token used by the SOC2 evidence collector to read app + secrets metadata. Set as a Fly se | `fly secrets set FLY_API_TOKEN=<value>` |
| `FLY_APP_NAME` | platform | prod | Fly.io application name. Required by auto-flipper apply mode (fly secrets set --app) and by the SOC2 | `fly secrets set FLY_APP_NAME=<value>` |

## 2. Provider SDKs imported but not credentialed
These providers will silently fail or return stub responses until credentialed:

| Provider | Missing vars | Placeholder vars |
|---|---|---|
| Supabase (auth + DB) | `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` | - |
| Stripe | `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` | - |
| OpenAI | `OPENAI_API_KEY` | - |
| SendGrid (email) | `SENDGRID_API_KEY` | - |
| Mux (video) | `MUX_TOKEN_ID`, `MUX_TOKEN_SECRET` | - |
| Fly.io | `FLY_API_TOKEN` | - |
| Sentry | `SENTRY_DSN`, `SENTRY_AUTH_TOKEN` | - |

## 3. Env vars referenced in code but absent from `prod-switches.yml` (R108)
_None. Registry covers every `process.env.X` reference under `src/`._

---

## Quick-start: copy these into Fly

```bash
# Required switches:
fly secrets set DATABASE_URL=<value>   # platform
fly secrets set DIRECT_URL=<value>   # platform
fly secrets set RECENT_AUTH_SECRET=<value>   # auth
fly secrets set RECENT_AUTH_TTL_MS=<value>   # auth
fly secrets set SUPABASE_SERVICE_ROLE_KEY=<value>   # platform
fly secrets set SUPABASE_URL=<value>   # platform
fly secrets set FLY_API_TOKEN=<value>   # platform
fly secrets set FLY_APP_NAME=<value>   # platform
# Provider credentials:
fly secrets set SUPABASE_URL=<value>   # Supabase (auth + DB)
fly secrets set SUPABASE_SERVICE_ROLE_KEY=<value>   # Supabase (auth + DB)
fly secrets set STRIPE_SECRET_KEY=<value>   # Stripe
fly secrets set STRIPE_WEBHOOK_SECRET=<value>   # Stripe
fly secrets set OPENAI_API_KEY=<value>   # OpenAI
fly secrets set SENDGRID_API_KEY=<value>   # SendGrid (email)
fly secrets set MUX_TOKEN_ID=<value>   # Mux (video)
fly secrets set MUX_TOKEN_SECRET=<value>   # Mux (video)
fly secrets set FLY_API_TOKEN=<value>   # Fly.io
fly secrets set SENTRY_DSN=<value>   # Sentry
fly secrets set SENTRY_AUTH_TOKEN=<value>   # Sentry
```
